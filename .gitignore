# ============================================================
# Dependencies
# ============================================================
node_modules/
.npm
.yarn/cache
.pnp.*

# ============================================================
# Environment & Secrets — NEVER commit these
# ============================================================
.env
.env.local
.env.*.local
*.env
google-service-account.json
*-service-account.json
*.pem
*.key
*.p12

# ============================================================
# Real production infrastructure data — repo is public, never commit
# ============================================================
imageref/features/phpipam_MySQL_dump_2026-06-19.sql
imageref/features/yourdistrict.org zone.txt
imageref/features/Phone System.xlsx
imageref/features/DHCP/
imageref/features/2025-2026 Phone Directory.docx
*.pfx

# ============================================================
# Build artifacts
# ============================================================
frontend/dist/
frontend/.vite/
backend/dist/
dns-engine/dist/
*.tsbuildinfo

# ============================================================
# Logs
# ============================================================
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ============================================================
# Blocklist compiled data (large, regenerated at runtime)
# ============================================================
blocklists/compiled/
blocklists/cache/

# ============================================================
# Database
# ============================================================
*.sqlite
*.sqlite3
*.db

# ============================================================
# OS & Editor
# ============================================================
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
*~

# ============================================================
# PM2
# ============================================================
.pm2/

# ============================================================
# Docker — dev-only override (node --watch, live source mounts). Never
# tracked: Compose auto-applies this filename with zero opt-in, so if it
# were committed, every fresh clone (including a real production server)
# would silently run in dev mode instead of the built image. Keep your own
# copy locally, untracked.
# ============================================================
docker-compose.override.yml
docker-compose.override.yml.local

# ============================================================
# SSL / Certbot (managed on server, not in repo)
# ============================================================
/infrastructure/nginx/ssl/
/letsencrypt/

# ============================================================
# Test coverage
# ============================================================
coverage/
.nyc_output/
